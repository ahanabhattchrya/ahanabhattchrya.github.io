import styles from "./travel.module.css"

export default function Travel() {
    return (
        <main className={styles.main}>
            <p>This is gonna be empty up for a bit, hopefully not too long! :P</p>
        </main>
    );
  }